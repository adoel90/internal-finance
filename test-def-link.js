import { MedusaApp } from "@medusajs/framework/modules-sdk"
import { defineLink } from "@medusajs/framework/utils"
import { modules } from "./src/modules-config" // wait, modules-config doesn't exist, we saw this error.

// Let's just create a mock to test `defineLink` behavior using Medusa's internal code.
